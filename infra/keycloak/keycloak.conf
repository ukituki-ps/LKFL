# Database — используем postgres:5432 (Docker DNS)
db=postgres
db-url=jdbc:postgresql://postgres:5432/keycloak
db-username=lkfl
db-password=lkfl-dev

# Hostname — через nginx reverse proxy (HTTP для dev)
hostname=project.ukituki.tech
hostname-strict=false
hostname-strict-https=false
hostname-port=80
hostname-backchannel-dynamic=false
proxy-headers=xforwarded
http-enabled=true

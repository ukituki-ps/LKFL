# T2214.3 — Отчёт о выполнении

## Статус

выполнено

## Что сделано

1. **StubBadge.tsx** — индикатор заглушки (красный круг с "?", tooltip). Рендерится только в dev (`import.meta.env.DEV`).
2. **Dashboard.tsx** — полный реврайт по прототипу:
   - Приветствие + дата + статус пакета
   - 3 stat-карточки (баланс, активные льготы, до конца периода)
   - Активные льготы (список с бейджами статуса)
   - Лента событий (цветные точки)
   - Быстрые действия (grid 3×2)
3. **Points.tsx** — заглушка:
   - Зелёная карточка баланса (48px value)
   - Прогресс-бары по категориям
   - Транзакции с фильтрами (Все / Начисления / Списания)
4. **Documents.tsx** — заглушка:
   - Таблица: документ, тип, дата, статус, «Скачать»
   - Бейджи типов (Заявление — blue, Согласие — gray, Полис — blue)
5. **Support.tsx** — заглушка:
   - FAQ аккордеон (левая колонка, 5 вопросов)
   - Форма обращения (правая колонка)

## Файлы

| Файл | Действие |
|------|----------|
| `src/components/ui/StubBadge.tsx` | создан |
| `src/pages/Dashboard.tsx` | изменён (полный реврайт) |
| `src/pages/Points.tsx` | изменён (полный реврайт) |
| `src/pages/Documents.tsx` | изменён (полный реврайт) |
| `src/pages/Support.tsx` | изменён (полный реврайт) |

## Критерии приёмки

- [x] Dashboard: моки по прототипу (3 stat-карточки, льготы, события, быстрые действия)
- [x] Points: моки по прототипу (баланс, прогресс-бары, транзакции)
- [x] Documents: моки по прототипу (таблица с бейджами)
- [x] Support: моки по прототипу (FAQ + форма)
- [x] StubBadge на каждом заглушечном блоке
- [x] StubBadge не виден в production
- [x] Иконки из DS `AprilIcon*`
- [x] `lucide-react` НЕ добавлен в dependencies
- [x] Эмодзи в mock-данных Dashboard заменены на `AprilIcon*` (🎬→AprilIconGift, 💪→AprilIconDumbbell, 🏥→AprilIconHeart)

## Примечания

- Эмодзи в mock-данных Dashboard заменены на `AprilIcon*` из DS (в ходе аудита T2214)
- `AprilIconGift`, `AprilIconDumbbell`, `AprilIconHeart` доступны в v0.1.16 — используются напрямую

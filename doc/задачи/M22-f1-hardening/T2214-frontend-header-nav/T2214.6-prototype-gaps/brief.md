# T2214.6 — Исправление гэпов прототипа

## Родительская задача

T2214 — Frontend: прототип → код (header, дизайн, страницы-заглушки)

## Веха

M22-f1-hardening

## Тип

code

## Кратко

Прототип (`артефакты/Прототип ЛК физика(1).html`) и реализованный фронтенд расходятся по ряду структурных, визуальных и UX гэпов.

**Цель T2214.6:** устранить все гэпы, которые укладываются в scope F1 (рабочий каталог). Модалки/wizard'ы отложены на F2 (T2602 flow-engine).

## Зависимости

- **T2214.5** (DS upgrade) — нужен `@ukituki-ps/april-ui@0.1.16`
- **T2214.1** (Brand tokens) — нужны `--brand-*` переменные
- **T2214.2** (Shell + Header) — нужен layout
- **T2214.3** (Pages stubs) — страницы уже реализованы
- **T2214.4** (Catalog refactor) — каталог уже обновлён

---

## Зоны гэпов

### 🔴 Критичные (структура/функционал)

#### ГЭП-1: Страница детализации льготы `/catalog/:slug`

**Проблема:** `EngagementCard` ссылается на `/catalog/${engagement.slug}` — маршрут существует (Link отрисован), но страница **не реализована**. Клик ведёт на пустую/404.

**Что сделать:**
- Добавить маршрут `/catalog/:slug` в `routes/employee.tsx` и `App.tsx`
- Создать `src/pages/CatalogDetail.tsx` с layout по прототипу-модалке Benefit Detail:
  - Header: название льготы + провайдер + badge статуса
  - Body: описание, иконка, стоимость
  - Footer: кнопка «Подключить» (stub → модалка в F2) или «Уже активна»
- Для DMS-льгот — табы: «Условия» / «Полис» / «Клиники» (заглушки с StubBadge)
- Данные — моки на основе `BENEFIT_DATA` из прототипа

**Файлы:**
- Новые: `src/pages/CatalogDetail.tsx`
- Изменить: `src/App.tsx` (+ маршрут), `src/routes/employee.tsx` (+ entry)

---

#### ГЭП-2: Dashboard — Быстрые действия не соответствуют прототипу

**Проблема:** реализация показывает навигационные ссылки (Каталог, Документы, Поддержка, Бонусы, Обратная связь, Профиль). Прототип — DMS-ориентированные действия с реальным контекстом:

| Прототип (2×2 grid) | Реализация (3 колонки) |
|---|---|
| Добавить родственника к ДМС | Каталог льгот |
| Апгрейд ДМС | Мои документы |
| Купить мерч СДЭК | Поддержка |
| Записаться к психологу | Бонусы |
| Заявка на мат. капитал | Обратная связь |
| *(нет)* | Профиль |

**Что сделать:**
- Заменить `mockQuickActions` на набор из прототипа (5 элементов)
- Grid layout: 2 колонки (как в прототипе), не 3
- Контент кнопок:
  - `user-plus` → «Добавить родственника к ДМС»
  - `arrow-up-circle` → «Апгрейд ДМС»
  - `shopping-bag` → «Купить мерч СДЭК»
  - `brain` → «Записаться к психологу»
  - `baby` → «Заявка на мат. капитал от компании»
- При клике — показывать toast «Функция будет доступна после F2» (StubBadge не подходит для кнопок)

**Файлы:** изменить `src/pages/Dashboard.tsx`

---

#### ГЭП-3: Dashboard — Layout 2 колонки

**Проблема:** прототип размещает «Быстрые действия» в правой колонке (width 292px, фиксированная). Реализация — внизу на всю ширину.

**Что сделать:**
- Левая колонка (flex): Активные льготы + Лента событий
- Правая колонка (292px): Быстрые действия (из ГЭП-2)
- Убрать QuickActionsGrid из нижней части (перенести в правую колонку)

**Файлы:** изменить `src/pages/Dashboard.tsx`

---

#### ГЭП-4: Points — Layout side-by-side

**Проблема:** прототип — 2 колонки: слева баланс (зелёная карточка + категории), справа транзакции. Реализация — вертикальный стек.

**Что сделать:**
- `Group wrap="nowrap"`:
  - Левая колонка (320px): зелёная карточка баланса + прогресс-бары категорий внутри неё (как в прототипе — категории рендерятся внутри зелёного блока)
  - Правая колонка (flex): Card с фильтрами + транзакциями
- Перенести прогресс-бары категорий внутрь зелёной карточки баланса (не в отдельную белую карточку)

**Файлы:** изменить `src/pages/Points.tsx`

---

### ⚠️ Умеренные (визуал/UX)

#### ГЭП-5: Header — Balance pill стиль

**Проблема:** прототип — зелёная пилюля `background: #F0FDF4; border: 1px solid #BBF7D0; border-radius: 20px; color: #166534` + текст «N баллов». Реализация — Mantine Badge с иконкой.

**Что сделать:**
- Обновить `HeaderRight`: заменить Mantine Badge на кастомную пилюлю по стилям прототипа
- Добавить слово «баллов» после числа
- Border-radius 20px, зелёный фон/бордер

**Файлы:** изменить `src/components/layout/HeaderRight.tsx`

---

#### ГЭП-6: Dashboard — Stat card 1 зелёный фон

**Проблема:** прототип — первая stat-карточка с зелёным фоном (`background: var(--green)`, белый текст). Реализация — все три белые.

**Что сделать:**
- Стат-карточка «Баланс баллов» — зелёный фон, белый текст, как в прототипе
- CSS-класс или inline style: `background: var(--brand-green)`, `color: #fff`
- Subtitle: «+500 баллов в июне» (как в прототипе)

**Файлы:** изменить `src/pages/Dashboard.tsx`

---

#### ГЭП-7: Dashboard — Лента событий: время + иконки

**Проблема:** прототип — у каждого события: цветная иконка (в квадрате 30×30, border-radius 8px) + текст с **bold** + временная метка. Реализация — точка 8px, без времени.

**Что сделать:**
- Заменить цветные точки на иконки в цветных квадратах (30×30, border-radius 8px)
- Цвета: `ev-green` (#DCFCE7/#16A34A), `ev-yellow` (#FEF9C3/#CA8A04), `ev-blue` (#DBEAFE/#2563EB)
- Добавить временную метку под текстом события
- Выделять **bold** ключевые части текста (суммы, названия льгот)

**Файлы:** изменить `src/pages/Dashboard.tsx`

---

#### ГЭП-8: Каталог — Hover-эффект карточек

**Проблема:** прототип — hover на карточке: `translateY(-2px)` + усиленный shadow. Реализация — без hover.

**Что сделать:**
- Добавить CSS transition к `Card` в `EngagementCard`:
  ```css
  transition: transform 0.15s, box-shadow 0.15s;
  ```
- On hover: `transform: translateY(-2px)`, `box-shadow: 0 4px 16px rgba(0,0,0,0.1)`
- Реализовать через `onMouseEnter`/`onMouseLeave` или CSS `:hover`

**Файлы:** изменить `src/components/catalog/EngagementCard.tsx`

---

#### ГЭП-9: Documents — secondary doc-meta строка

**Проблема:** прототип — у каждого документа под именем: мелкая строка «АльфаСтрахование · ДМС». Реализация — только имя.

**Что сделать:**
- Добавить `doc-meta` поле в моки (строка под названием)
- Рендерить как `Text size="xs" c="dimmed"` под именем документа

**Файлы:** изменить `src/pages/Documents.tsx`

---

#### ГЭП-10: Documents — кнопка «Скачать» вместо иконки

**Проблема:** прототип — кнопка с текстом «Скачать» + иконка download. Реализация — ActionIcon с tooltip.

**Что сделать:**
- Заменить `ActionIcon` на `Button variant="subtle" size="xs"` с текстом «Скачать» + иконка

**Файлы:** изменить `src/pages/Documents.tsx`

---

#### ГЭП-11: Support — Success state формы

**Проблема:** прототип — после сабмита: скрывается форма, показывается блок «Обращение отправлено!» с иконкой и текстом. Реализация — форма без onSubmit handler.

**Что сделать:**
- Добавить `useState` для tracking формы/успеха
- `onSubmit`: скрыть форму, показать success block (иконка check-circle + «Обращение отправлено!» + «Мы ответим в течение 1 рабочего дня»)
- Кнопка «Отправить» → вызывать handler

**Файлы:** изменить `src/pages/Support.tsx`

---

#### ГЭП-12: Dashboard — Stat card 3 формат

**Проблема:** прототип — «47 дн» (число крупно + «дн» мелко). Реализация — «18 дней» (одной строкой).

**Что сделать:**
- Разбить на `value` (число, 26px fw:800) + `suffix` («дн», 16px fw:600)
- Подпись: «Период: янв — июн 2025» (как в прототипе)

**Файлы:** изменить `src/pages/Dashboard.tsx`

---

## НЕ в scope T2214.6

Эти гэпы отложены на F2 (M25–M29):

| ГЭП | Веха | Причина |
|-----|------|---------|
| DMS Wizard (4 шага: опция → оплата → подтверждение → готово) | F2, T2602 | Требует flow-engine, billing, consent |
| Mat Capital Wizard (4 шага) | F2, T2602 | Требует flow-engine, billing |
| Benefit Detail Modal (табы: условия / полис / клиники на карте) | F2 | Требует API данных провайдера, карта клиник |
| Модалка активации льготы | F2, T2602 | Требует flow-engine |
| Реальная карта клиник (OpenStreetMap) | F3 | Требует интеграции с провайдером |

---

## Файлы (сводка)

### Изменяются
| Файл | Гэпы |
|------|------|
| `src/pages/Dashboard.tsx` | ГЭП-2, ГЭП-3, ГЭП-6, ГЭП-7, ГЭП-12 |
| `src/pages/Points.tsx` | ГЭП-4 |
| `src/pages/Documents.tsx` | ГЭП-9, ГЭП-10 |
| `src/pages/Support.tsx` | ГЭП-11 |
| `src/components/layout/HeaderRight.tsx` | ГЭП-5 |
| `src/components/catalog/EngagementCard.tsx` | ГЭП-8 |
| `src/App.tsx` | ГЭП-1 (+ маршрут) |
| `src/routes/employee.tsx` | ГЭП-1 (+ entry) |

### Новые
| Файл | Гэп |
|------|-----|
| `src/pages/CatalogDetail.tsx` | ГЭП-1 |

---

## Критерии приёмки

- [ ] **ГЭП-1:** `/catalog/:slug` рендерится, не падает в 404. Показывает название, провайдер, описание, стоимость, кнопку.
- [ ] **ГЭП-2:** Быстрые действия — 5 кнопок из прототипа, grid 2 колонки, при клике — toast «Функция будет доступна после F2».
- [ ] **ГЭП-3:** Dashboard — 2 колонки: слева льготы + события, справа быстрые действия. Без нижнего блока.
- [ ] **ГЭП-4:** Points — 2 колонки: слева баланс + категории (внутри зелёной карточки), справа транзакции.
- [ ] **ГЭП-5:** Balance pill в header — зелёный фон, border, текст «N баллов».
- [ ] **ГЭП-6:** Stat card «Баланс баллов» — зелёный фон, белый текст.
- [ ] **ГЭП-7:** События — иконки в цветных квадратах, временные метки, bold в тексте.
- [ ] **ГЭП-8:** Карточки каталога — hover с translateY(-2px) + shadow.
- [ ] **ГЭП-9:** Документы — secondary строка под названием (провайдер · контекст).
- [ ] **ГЭП-10:** Документы — кнопка «Скачать» с текстом + иконка.
- [ ] **ГЭП-11:** Support — success state после сабмита формы.
- [ ] **ГЭП-12:** Stat card 3 — число + «дн» мелким шрифтом, подпись с периодом.
- [ ] `npm run dev` → визуально соответствует прототипу (в рамках scope F1)
- [ ] Unit-тесты не ломаются (или обновлены)
- [ ] StubBadge на заглушечных блоках (CatalogDetail, модалки F2)

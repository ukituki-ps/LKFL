# T1703 — Makefile

## Прогресс

0%

## Checklist

- [ ] Создать `Makefile` с targets: build, test, lint, docker, migrations, dev
- [ ] `.PHONY` на всех targets
- [ ] DB_DSN из `.env`
- [ ] `make build` — собирает lkfl-server
- [ ] `make test` — запускает тесты с race detector
- [ ] `make up` — docker compose up
- [ ] `make lint` — golangci-lint
- [ ] `make migrate-up` — atlas migrate apply
- [ ] Заполнить report.md

## Зависимости

depends_on: []

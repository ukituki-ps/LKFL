# T1704 — DI Wiring (app/)

## Прогресс

0%

## Checklist

- [ ] Создать `app/config.go` — Config struct + viper loading + validation
- [ ] Создать `app/wire.go` — Provide() функция
- [ ] Создать `app/server.go` — chi router + middleware chain
- [ ] Config: `.env` → ENV → defaults
- [ ] DB pool (pgxpool) с connection limits
- [ ] Redis client (go-redis/v9) с retry
- [ ] OIDC verifier (go-oidc)
- [ ] Logger (structured JSON)
- [ ] Prometheus custom registry
- [ ] CORS configurable
- [ ] `/healthz` → 200 OK
- [ ] `/metrics` → Prometheus endpoint
- [ ] Заполнить report.md

## Зависимости

depends_on: [T1701]

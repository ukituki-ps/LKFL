# T1706 — cmd/server/main.go (HTTP entry point)

## Прогресс

0%

## Checklist

- [ ] Создать `cmd/server/main.go`
- [ ] Load config → Provide() → Start server
- [ ] Graceful shutdown (SIGINT/SIGTERM, 30s timeout)
- [ ] Error handling (os.Stderr + exit 1)
- [ ] `go run ./cmd/server/` запускает server
- [ ] `/healthz` → 200 OK
- [ ] SIGINT → graceful shutdown
- [ ] Config error → exit 1
- [ ] Заполнить report.md

## Зависимости

depends_on: [T1704]

# T1207 — Финальная консистентность

## Контекст

После T1201-T1206 документация частично обновлена, но 23 ADR файла содержат упоминания микросервисов, NATS и отдельных сервисов. Нужно финальный pass по всем файлам.

## План

1. Пройти по ADR 001-023 → если упоминает "отдельный сервис billing" / NATS / :8081-:8085 → добавить note "M12: merged into modular monolith, see ADR-024"
2. Обновить диаграмму системы в `архитектура/README.md` — 4 сервиса → один бинарник
3. Обновить `задачи/README.md` — добавить M12 в таблицу вех
4. Grep check: `:8081 | :8082 | :8084 | :8085` → 0 mentions in .md files
5. Grep check: `"billing service"` → заменить на "billing package"
6. Пройти по `спецификация/критерии-приёмки.md` → нет ли "billing service up" exit criteria

## Ожидаемый результат

Вся документация консистентна — ноль упоминаний отдельныx сервисов где они больше не существуют. Диаграмма переделана.
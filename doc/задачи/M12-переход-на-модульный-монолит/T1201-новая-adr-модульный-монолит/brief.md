# T1201 — ADR-024: Modular Monolith rationale

## Контекст

Планarchitecture описывает 4 Go-сервиса (platform, billing, integrations, payment-gateway) с NATS JetStream для межсервисной коммуникации. Однако проект разрабатывается с использованием LLM-агентов, а не распределённой командой разработчиков.

Микросервисная архитектура создаёт overhead без benefits при агентной разработке:
- NATS contracts не типизированы и не проверяются компилятором → тихие баги
- Агент тратит 8.7K токенов контекста на 1K токенов кода (читает doc + код другого сервиса)
- 4 go.mod, 4 Dockerfile, 14+ контейнеров в docker-compose → infra noise
- `go build ./...` → 4 модуля, 4 результата, 4 возможных ошибки

## План

Создать ADR-024 в формате ХАДД (Контекст → Рассмотренные варианты → Решение → Последствия), обосновывающий переход на modular monolith.

## Ожидаемый результат

- `doc/архитектура/adr/024-modular-monolith.md` — полноценная ADR
- `doc/архитектура/README.md` — обновлена таблица ADR (добавлена строка для ADR-024)

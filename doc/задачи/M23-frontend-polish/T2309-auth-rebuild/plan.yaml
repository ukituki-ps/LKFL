# T2309 — Пересборка Logout: гибридный подход

## Прогресс

100%

## Checklist

### Шаг 1: Rewrite `Logout()` в handler.go

- [x] Удалить строки 459-545 (коррумпированный Logout)
- [x] Написать чистую `Logout()` с гибридным подходом
- [x] Вынести логику в хелперы: `resolvePostLogoutRedirect()`, `buildKeycloakLogoutURL()`
- [x] Порядок: invalidateKeycloakSSO → Redis cleanup → cookie cleanup → redirect

### Шаг 2: Хелпер `resolvePostLogoutRedirect`

- [x] Query param → Origin → Referer → default
- [x] Валидация через `isValidPostLogoutRedirect()`

### Шаг 3: Хелпер `buildKeycloakLogoutURL`

- [x] publicIssuer + realmPath + /protocol/openid-connect/logout
- [x] client_id + post_logout_redirect_uri query params

### Шаг 4: Удалить дубликаты

- [x] Дублирующий invalidateKeycloakSSO call
- [x] Дублирующий tokenStore.Delete call
- [x] Нарушенная нумерация комментариев

### Шаг 5-7: Проверить/переписать вспомогательные функции

- [x] `invalidateKeycloakSSO()` — двухуровневый подход (Admin API + POST fallback)
- [x] `getAdminToken()` — client_credentials grant
- [x] `fetchAdmin()` — GET к Admin REST API

### Шаг 8: Сборка

- [x] `go build ./...` без ошибок
- [x] `go vet ./...` без ошибок
- [x] `tsc --noEmit` без ошибок

### Шаг 9: Frontend authStore — не менять

- [x] Подтвердить, что logout() правильный

### Шаг 10: Unit-тесты frontend

- [x] `npm test -- --run` — 111/111 passed

### Шаг 11: Интеграционный тест

- [x] Усилить TestLogout: проверить `Location` header содержит `protocol/openid-connect/logout` (уже в T2308)
- [x] Усилить TestLogout: проверить `post_logout_redirect_uri=` в Location header (уже в T2308)
- [x] Усилить TestLogout: проверить `client_id=` в Location header (уже в T2308)
- [x] Заменить `t.Logf` на `t.Errorf` для проваленных проверок (уже в T2308)

### Шаг 12: E2E тест

- [x] Сделать обработку confirmation page устойчивой: try/wait 3s → если #kc-logout видим, кликнуть (case A); если не видим, OK (case B — SSO мертва, redirect автоматический) (уже в T2308)
- [x] Проверить KAUTH_SESSION_ID удалена (уже в T2308)
- [x] Проверить SSO инвалидирована (Keycloak показывает форму логина) (уже в T2308)

### Шаг 13: Обновить риски

- [x] R-042 → гибридный logout

### Шаг 14-15: Документация

- [x] report.md
- [x] plan.yaml

## Порядок выполнения

1. Шаг 1-3 (backend Logout rewrite + хелперы) ✅
2. Шаг 4 (удаление дубликатов — автоматически при rewrite) ✅
3. Шаг 5-7 (проверка вспомогательных функций) ✅
4. Шаг 8 (go build + go vet + tsc --noEmit) ✅
5. Шаг 9-10 (frontend проверка) ✅
6. Шаг 11 (интеграционный тест — уже усилен в T2308) ✅
7. Шаг 12 (E2E тест — уже устойчив в T2308) ✅
8. Шаг 13-15 (документация) ✅

# T2310 — CI/CD Pipeline Optimization

## Прогресс

100% — все рекомендации реализованы и валидированы

## Checklist

### R1: Go cache (критический — экономия ~9m)

- [x] Добавлен step `Init Go cache directory` — создаёт `/home/ukituki/go-cache/{mod,build}`
- [x] Отключить GitHub cache API: `cache: false` в `actions/setup-go@v5`
- [x] Установить `GOMODCACHE=/home/ukituki/go-cache/mod` и `GOCACHE=/home/ukituki/go-cache/build` в workflow env
- [x] Проверено: Lint & Test за 52s (было 13.6m) — cache работает

### R2: Docker build cache (local filesystem)

- [x] Использовать `cache-from`/`cache-to` type=local через `/home/ukituki/docker-cache/`
- [x] `driver: docker` (docker-container нестабилен на self-hosted runner)
- [x] `max-parallel: 2` (RAM ограничения сервера)
- [x] Добавлен step `Update Docker cache` — atomic cache replacement

### R3: golangci-lint cache (экономия 10-20s)

- [x] `skip-cache: false` — кэш включён

### R4: E2E Staging — polling вместо sleep 30

- [x] Заменён `sleep 30` на polling OIDC discovery endpoint (3s, 20 attempts)

### R5: Node.js 24 migration

- [x] Добавлено `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'` в workflow env
- [x] Убран невалидный `cache: false` из всех `actions/setup-node@v4`

### Валидация

- [x] Pipeline запущен на main — все job'ы прошли (run #26830666625, success)
- [x] Среднее время ~11m (было ~25m) — **-57%**
- [x] Go cache warm — Lint & Test за 52s (было 13.6m)
- [x] Все 4 Docker build прошли (server 1m2s, proxy 56s, frontend 1m17s, deploy-worker 53s)
- [x] Deploy Staging + Smoke Test прошли
- [x] E2E Integration + E2E Staging прошли (continue-on-error, как и раньше)

## Системные исправления на сервере

- [x] `/etc/docker/daemon.json` — ipv6: false + DNS 8.8.8.8
- [x] Docker build cache очищен (42.3GB → 0B)
- [x] Postgres password исправлен (staging)
- [x] Go cache директории созданы (`/home/ukituki/go-cache/`)
- [x] Docker cache директории созданы (`/home/ukituki/docker-cache/`)

depends_on: []

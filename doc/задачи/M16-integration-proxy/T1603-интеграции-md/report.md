# T1603 — Отчёт: `архитектура/интеграции.md` rewrite

## Веха

M16-integration-proxy

## Что сделано

Переписан документ `doc/архитектура/интеграции.md` под архитектуру с Integration Proxy (ADR-035).

### Изменения по секциям

| Секция | Что изменено |
|--------|-------------|
| **TL;DR** | Обновлены ссылки: добавлены строки для proxy-архитектуры, gRPC Contract, ProviderAdapter в proxy |
| **Краткое описание** | Переписан: Integration Proxy вместо `internal/integrations/` direct call. Добавлена ссылка на ADR-035 и M16 |
| **Архитектура** | Новая diagram: Nginx → mono (:8080) + proxy (:8090 gRPC, :8091 HTTP) → провайдеры. Удалена старая диаграмма монолита |
| **Принцип «Единый шлюз + Кэш»** | Переписан: шлюз = proxy, кэш в `lkfl_integration`, асинхронная активация через worker pool |
| **Инвентарь интеграций** | Без изменений (каталоги провайдеров те же) |
| **ProviderAdapter interface** | Добавлена пометка: реализация в `integration-proxy/adapters/`, не в монолите |
| **gRPC Contract** | Сохранён без изменений (добавлен T1602) |
| **Generic REST adapter** | Добавлена пометка: YAML config загружается proxy. Обновлён «Как работает» — proxy читает YAML, не монолит |
| **Admin endpoints** | Переписан: mono делегирует к proxy через gRPC. Добавлена таблица gRPC методов. Источник данных — `lkfl_integration` |
| **Обработка ошибок** | Переписан: proxy returns structured errors via gRPC. Добавлена таблица gRPC status → HTTP mapping |
| **Синхронизация каталога** | Переписан: proxy worker, не mono Asynq. Каталог в `lkfl_integration`. Добавлен flow синхронизации |

### Что НЕ изменено

- Инвентарь интеграций (все 10 категорий провайдеров)
- Секция "gRPC Contract" (добавлена T1602)
- Контракты провайдеров (универсальные API контракты)

### Проверка

- [x] 0 ссылок на прямые вызовы из монолита к провайдерам льгот
- [x] Консистентность с ADR-035 (архитектура, термины, диаграмма)
- [x] gRPC Contract секция сохранена
- [x] Инвентарь интеграций сохранён
- [x] Пэйшлюз остаётся в монолите (`internal/payments/`, не через proxy) — PCI DSS

## Время

~40 минут

## Замечания

- Пэйшлюз (payments) остаётся в монолите — не затрагивается proxy (ADR-018, PCI DSS)
- HR Sync остаётся в монолите — не проходит через proxy
- 1С Payroll остаётся в монолите — не проходит через proxy

# Exit Criteria — по вехам (M00 → M17)

## TL;DR (для агентов)

> Exit criteria для каждой вехи. ~280 строк.
> - **M01–M03 (документация):** строка 10 | **M04–M07 (архитектура):** строка 60
> - **M08–M13 (CEL/gamification/survey/mono):** строка 110
> - **M14 (отменена):** строка 180
> - **M15 (завершено):** строка 211
> - **M16 (завершено — Integration Proxy):** строка 226
> - **M17 (код — Authorization + CI/CD, эпик + 5 задач):** строка 243

> **Связь:** `план/вехи.md` → описание вех. `спецификация/критерии-приёмки.md` → детальные критерии по артефактам.

---

## M01 — Создание описания

### Что должно быть готово

- [x] `контекст/философия.md` — «Три нуля» (white-label, multi-tenant, no-hardcoded)
- [x] `контекст/проблема.md` — 12 бизнес-проблем с обоснованием
- [x] `контекст/акторы.md` — 10 акторов (5 людей, 3 системных, 2 внешних)
- [x] `контекст/ограничения.md` — 11 NFR-ограничений
- [x] `контекст/negative-criteria.md` — 10 «НЕ делаем»
- [x] `архитектура/модули.md` — горизонтальное описание (4 сервиса + Frontend)
- [x] `архитектура/стек.md` — технологии с обоснованием
- [x] `архитектура/интеграции.md` — N провайдеров
- [x] `архитектура/безопасность.md` — Keycloak, RBAC, TLS, 152-ФЗ
- [x] `архитектура/adr/` — первые 4 ADR (ХАДД)
- [x] `спецификация/артефакты.md` — таблица артефактов
- [x] `спецификация/api.md` — REST API endpoints
- [x] `спецификация/journeys/` — 5 файлов journeys
- [x] `спецификация/критерии-приёмки.md` — 61 критерий

**Exit criteria:** вся базовая документация покрыта, нет пробелов в разделах Контекст → Архитектура → Спецификация.

---

## M02 — Улучшение документации

- [x] Banner «Примеры — СДЭК» во всех journey-файлах (сотрудник, hr, менеджер-каталога)
- [x] `настраиваемость.md` — сводная таблица: что настраивается без кода vs требует код
- [x] Schema benefit/eligibility/plan — детальные поля и типы
- [x] Диаграмма потоков активации (activation flow sequence diagram)
- [x] `tenant-onboarding.md` — пошаговый walkthrough (2 часа)
- [x] UI description для activity types

**Exit criteria:** документация покрывает white-label edge-кейсы, новый разработчик может понять настраиваемость без кода.

---

## M03 — Архитектура льгот

- [x] Eligibility engine спроектирован (AND/OR/groups)
- [x] Tariff model (plans) — N тарифов на 1 продукт
- [x] Flow-based activation — шаги: form → approval → provider_api → document_generation
- [x] M03 отражена в `архитектура/модули.md`

**Exit criteria:** архитектура льгот поддерживает многослойную модель: eligibility check → tariff selection → multi-step activation → provider integration → document generation.

---

## M04 — API под льготы

- [x] API: `/benefit-plans/` CRUD (2 endpoints)
- [x] API: `/user-benefits/` activate/deactivate/upgrade (7 endpoints)
- [x] API: `/admin/benefit-plans/` CRUD (6 endpoints)
- [x] API: `/admin/activation-flows/` CRUD (5 endpoints)
- [x] `спецификация/api.md` v2: 128 endpoints (было 116)
- [x] `спецификация/артефакты.md` обновлены (новые артефакты планов)

**Exit criteria:** API контракты покрывают M03-архитектуру. +12 нетто endpoints.

---

## M05 — Унификация Engagement

- [x] Benefit + Activity → единый EngagementType
- [x] Benefit Plan + Activity Instance → EngagementOffer
- [x] UserBenefit + Completion → UserEngagement
- [x] API: `/engagements/` с `?type=benefit|activity`
- [x] API: `/user-engagements/` универсальный
- [x] RBAC guard: hr→activity, catalog_manager→benefit
- [x] Journeys обновлены: J02, J04, J05, J07, J12 используют engagement terminology
- [x] Критерии приёмки обновлены
- [x] `архитектура/активности.md` → redirect к engagement.md
- [x] `архитектура/льготы.md` → redirect к engagement.md

**Exit criteria:** единая модель engagements. benefit и activity — два типа одного артефакта. Нет legacy ссылок на отдельные benefit/activity эндпоинты.

---

## M06 — Разбиение Platform

- [x] Platform → 7 internal пакетов: engagement, eligibility, notification, recommendations, gamification, billing, integrations
- [x] Каждый пакет с публичным API и зависимостями
- [x] DI graph (dependency injection) между пакетами описан
- [x] `архитектура/пакеты-platform.md` — описание всех 7 пакетов
- [x] Unit-test isolation criteria определены (критерии 57-61)

**Exit criteria:** Platform = 7 пакетов. Каждый тестируемо изолирован.

---

## M07 — Аудит и рефакторинг

- [x] Eligibility → отдельный пакет `internal/eligibility/` (было внутри engagement/)
- [x] Compliance (cascade + audit + retention) → `internal/compliance/`
- [x] admin_handler.go → 5 файлов (admin_user, admin_catalog, admin_recommendations, admin_analytics, admin_content)
- [x] Generic REST adapter + YAML config (замена 11 hard-coded adapters)
- [x] Payment Gateway service (PCI DSS, 4-й сервис → позже merged в M12)
- [x] JSON-driven Wizard engine (замена DmsWizard + MatKapitalWizard)
- [x] NATS master registry (→ позже superseded M12)
- [x] Integrations Hub admin API (12 endpoints)
- [x] 9 internal пакетов (было 7 → +eligibility, +compliance)
- [x] Cross-reference consistency audit

**Exit criteria:** 9 пакетов. Admin split. Generic adapter. Wizard engine. Integrations Hub API документирован.

---

## M08 — CEL и LLM Engine

- [x] CEL Engine заменяет 4 механизма: eligibility, recommendations, billing conditions, flow conditions
- [x] LLM генерация CEL из русского текста
- [x] 3-фазная миграция: Phase 1 (eligibility→CEL), Phase 2 (recoms, billing), Phase 3 (flow, dry-run)
- [x] DB migrations: `condition_cel`, `condition_json` (shadow)
- [x] ADR-021 (CEL Engine), ADR-022 (LLM Proxy)
- [x] TagResolver спроектирован (теги пользователя)

**Exit criteria:** CEL Engine архитектуры завершена. 5 доменов правил: eligibility, recommendations, billing, flow, gamification.

---

## M09 — Gamification System

- [x] AchievementGrantEngine: CEL-based условия присвоения ачивок
- [x] LoyaltyLevelManager: уровни лояльности на основе engagement activity
- [x] Trigger bonuses: автоматические бонусы при определённых событиях
- [x] XLSX импорт массовых ачивок
- [x] Game context builder → CEL context extension (game.*)
- [x] ADR-023 (Gamification)

**Exit criteria:** Gamification спроектирована как 5-й домен CEL Engine. Интегрируется через game.* tags в CELContext.

---

## M10 — Рефакторинг по результатам аудита

- [x] Recommendations → stub (Phase 2). Phase 1: 2-layer rule engine (context + segment)
- [x] LLM Proxy :8085 merged в `internal/llm/` (in-process)
- [x] CELContext builder вынесен в `shared/pkg/celcontext`
- [x] API router разделён по доменам
- [x] JWT middleware → `shared/pkg/auth`
- [x] 11 internal пакетов (добавили llm/, recommendations stub)

**Exit criteria:** LLM in-process. Recommendations stub. shared auth + CELContext. Router domain-split.

---

## M11 — Split Integrations

- [x] ProviderGateway → только provider adapters (без HR, без payroll)
- [x] HR Sync → Platform `user/HRSync` (bypasses Integrations Hub)
- [x] 1C Payroll → Billing `payroll/` (bypasses Integrations Hub)
- [x] NATS namespace rename: `integration.*` → `provider.*`
- [x] ADR-026, ADR-027, ADR-028

**Exit criteria:** Integrations Hub чистый (только provider adapters). HR/payroll внутри Platform.

---

## M12 — Модульный монолит

- [x] ADR-024: Modular Monolith
- [x] 4 Go-сервиса (Platform, Billing, Integrations, Payment Gateway) → 1 бинарник `lkfl-server`
- [x] 17 internal-пакетов (15 business + tenant + api): tenant/, auth, user, consent, cel, llm, eligibility, compliance, engagement, notification, gamification, billing, integrations, payments, content, api, recommendations/ (stub)
- [x] Go interfaces вместо NATS для межмодульных вызовов
- [x] API: `/billing/v1/*` → `/api/v1/billing/*`
- [x] API: `/payments/*` → `/api/v1/payments/*`
- [x] NATS → optional dependency (future microservice split)
- [x] 134 Platform endpoints + 12 Integrations Hub = 146 total
- [x] Health: 4 endpoint'а → 1 `/api/healthz`
- [x] Финальная консистентность всех файлов

**Exit criteria:** 1 бинарник. 17 internal-пакетов (15 business + tenant + api). Go interfaces. NATS не используется. Все сервисы → internal.

---

## M13 — Survey Engine

- [x] Survey schema: question → option → branch_on → scoring
- [x] SurveyEngine API: Create/GetResults/Analytics
- [x] TagMapper: survey results → user survey tags
- [x] FlowEngine integration: survey как step type
- [x] Analytics API: per-question, per-branch, response rate
- [x] DB: surveys, survey_questions, survey_responses, survey_branches (4 таблицы)
- [x] ADR-025 (Survey Engine)

**Exit criteria:** Survey Engine архитектуры завершена. Ready для реализации.

---

## M14 — Survey Implementation

> **⛔ ОТМЕНЕНА.** Решение команды — отложить реализацию кода. Архитектура (M13) сохранена.

**Exit criteria:** N/A (веха отменена).

---

## M15 — Архитектура фронтенда

- [x] `архитектура/фронтенд.md` создан (10 разделов: A→J, §§I-J краткие + ссылка на `фронтенд-mobile-forms.md`)
- [x] `архитектура/фронтенд-mobile-forms.md` создан (Mobile + Forms детально)
- [x] 4 ADR созданы и приняты (ADR-031 → ADR-034)
- [x] ADR-029 обновлён (11 → 2 реальных gap'а)
- [x] Маппинг прототипа завершён — 0 расхождений
- [x] Mobile-архитектура: `AprilMobileShellBar`, breakpoints, touch-ориентиры, жесты
- [x] Forms-архитектура: Zod + react-hook-form, wizard, survey, admin-формы
- [x] NAVIGATION.md + архитектура/README.md + модули.md обновлены

**Exit criteria:** Полная архитектурная документация фронтенда: `фронтенд.md` (10 разделов), `фронтенд-mobile-forms.md`, 4 ADR (031→034), обновлённый ADR-029, актуализированная навигация.

---

## M16 — Integration Proxy

- [x] ADR-035: Integration Proxy создан
- [x] gRPC proto definition (integration.proto)
- [x] интеграции.md rewrite под proxy
- [x] пакеты-platform.md: −integrations/, +integrationclient/, +proxy
- [x] schema.md: +lkfl_integration (6 таблиц)
- [x] api.md: admin endpoints → proxy delegation
- [x] модули.md: +proxy binary, diagram
- [x] стек.md: +gRPC, +6 метрик
- [x] безопасность.md: credential isolation, STRIDE
- [x] NAVIGATION.md + README.md update
- [x] плановые файлы обновлены
- [x] контекстные файлы обновлены
- [x] Final consistency check

**Exit criteria:** Proxy архитектура задокументирована. 21 файл обновлён. 0 рассинхронов.

---

## M17 — Authorization (код)

> Разбита на 5 задач: T1701–T1705. T1700 — эпик (только в документации).

### T1701 — Инфраструктура и bootstrap

- [ ] `go.mod` инициализирован с зависимостями
- [ ] `cmd/server/main.go` — stub (log + os.Exit(0)) для go build
- [ ] `cmd/integration-proxy/main.go` — stub (log + os.Exit(0)) для go build + Dockerfile.proxy
- [ ] `docker-compose.yml` — PostgreSQL 17, Redis 7, Keycloak 26.x, Nginx
- [ ] `Makefile` — make run, make build, make test, make migrate, make docker-up
- [ ] CI smoke check — `.github/workflows/go.yml` + `frontend.yml` (заменяются на ci.yml в T1704)
- [ ] `infra/keycloak/realm/tenant-template.json` — realm per tenant шаблон
- [ ] `infra/keycloak/scripts/create-tenant-realm.sh` — скрипт создания realm
- [ ] `infra/nginx/lkfl.conf` — route table, security headers, SPA fallback

**Exit criteria T1701:**
- `go mod tidy` без ошибок
- `go build ./...` компилирует оба бинарника (stub server + stub proxy)
- `make docker-up` поднимает все сервисы
- Keycloak доступен, realm создаётся из template
- CI smoke check: go build + frontend build на push/PR

### T1702 — Backend auth core

- [ ] `shared/pkg/auth/` — 7 файлов (resolver, verifier, middleware, rbac, claims, errors, cache)
- [ ] `internal/auth/` — OIDCVerifier wrapper, tenant config builder
- [ ] `internal/tenant/` — CRUD, brand config, repository, service
- [ ] `internal/api/` — router с middleware chain, route registration
- [ ] Migrations: только auth-таблицы (tenants, tenant_brand, users) — остальные 44 таблицы в будущих вехах
- [ ] Seed Keycloak: демо realm "demo" + пользователи admin/employee + clients lkfl-frontend/lkfl-server
- [ ] Seed data: демо tenant (slug: `demo`), демо users с ролями
- [ ] `cmd/server/main.go` — DI, middleware chain, graceful shutdown
- [ ] `cmd/worker/main.go` — Asynq worker stub

**Exit criteria T1702:**
- `lkfl-server` компилируется: `go build ./...`
- `/api/v1/me` → 200 с профилем и ролями
- `/api/v1/healthz` → 200
- Tenant resolution по subdomain
- JWT валидация с realm-specific JWKS
- RBAC guard блокирует без нужных ролей

### T1703 — Frontend auth

- [ ] Vite bootstrap + package.json
- [ ] `src/keycloak.ts` — Keycloak instance, init, config
- [ ] `src/keycloak-token-subscribers.ts` — token rotation notification
- [ ] `src/api/platform.ts` — authorizedFetch (401 retry + refresh), React Query
- [ ] `src/store/auth.ts` — Zustand auth store
- [ ] `src/App.tsx` — auth state machine
- [ ] `src/main.tsx` — Sentry init, auth init, render
- [ ] Auth guards: RequireAuth, GuestZone, ForbiddenZone, AuthorizedShell
- [ ] Layout: Header, Sidebar, PageLayout
- [ ] Страницы stub: Dashboard, Catalog
- [ ] Unit-тесты (vitest): store/auth.ts, api/platform.ts, App.tsx — все зелёные

**Exit criteria T1703:**
- Frontend запускается: `npm run dev`
- keycloak-js инициализируется
- Login flow: guest → login → transition → authorized
- authorizedFetch retry при 401 + token refresh
- Unit-тесты (vitest): store, api, App — все зелёные

### T1704 — CI/CD pipeline (Фаза A: Dockerfile+Actions; Фаза B: testcontainers+OpenAPI)

- [ ] `.github/workflows/ci.yml` — lint + test + build + Playwright E2E на PR
- [ ] `.github/workflows/cd.yml` — docker build + push ghcr.io + integration test на merge
- [ ] `.github/workflows/deploy.yml` — dev deploy (auto), staging (manual)
- [ ] `Dockerfile.server` — multi-stage (golang → alpine)
- [ ] `Dockerfile.proxy` — multi-stage (golang → alpine)
- [ ] `Dockerfile.frontend` — multi-stage (node → nginx)
- [ ] `internal/testutil/containers.go` — PG + Redis + Keycloak testcontainers
- [ ] `*_integration_test.go` — integration тесты с testcontainers
- [ ] `openapi/openapi.yaml` — master spec
- [ ] `docker-compose.prod.yml` — production конфигурация

**Exit criteria T1704:**
- Фаза A: 3 Dockerfile (server, proxy stub, frontend) проходят multi-stage build
- Фаза A: `.github/workflows/ci.yml` запускается на PR (lint + test + build + Playwright E2E)
- Фаза A: `.github/workflows/cd.yml` — docker build + push ghcr.io на merge
- Фаза B: Integration tests проходят через testcontainers
- Фаза B: OpenAPI spec валидируется в CI

### T1705 — Observability (зависит от T1701; можно отложить до M18)

- [ ] `docker-compose.yml` — +prometheus, +grafana, +loki
- [ ] `infra/prometheus/prometheus.yml` — scrape configs
- [ ] `infra/grafana/provisioning/` — auto-provisioned dashboards
- [ ] `infra/grafana/dashboards/` — JSON: Platform Overview, Backend Metrics, Infrastructure, Logs
- [ ] Loki в docker-compose — Docker logs driver → Loki
- [ ] Grafana Explore — filter по tenant_id, level, svc

**Exit criteria T1705:**
- `make docker-up` включает Prometheus + Grafana + Loki
- Grafana показывает дашборды Platform Overview, Backend Metrics, Infrastructure
- Логи собираются в Loki, доступны через Grafana Explore

### Общий Exit criteria M17 (сборка из T1701–T1705)

- `lkfl-server` компилируется: `go build ./...` ✅
- `go test ./...` — все зелёные ✅
- docker-compose поднимается: `make docker-up` ✅
- Login flow работает: guest → login → authorized ✅
- `/api/v1/me` → 200 с профилем и ролями ✅
- Tenant resolution работает по subdomain ✅
- JWT валидация с realm-specific JWKS ✅
- RBAC guard блокирует без нужных ролей ✅

---

## Общий чек-лист

| Веха | Статус | Задач | % завершено |
|------|--------|-----:-|:----------:|
| M01 | ✅ | 4 | 100% |
| M02 | ✅ | 6 | 100% |
| M03 | ✅ | 1 | 100% |
| M04 | ✅ | 1 | 100% |
| M05 | ✅ | 3 | 100% |
| M06 | ✅ | 1 | 100% |
| M07 | ✅ | 9 | 100% |
| M08 | ✅ | 1 | 100% |
| M09 | ✅ | 1 | 100% |
| M10 | ✅ | 5 | 100% |
| M11 | ✅ | 4 | 100% |
| M12 | ✅ | 7 | 100% |
| M13 | ✅ | 1 | 100% |
| M14 | ⛔ | 1 (код) | ⛔ |
| M15 | ✅ | 9 (doc) | 100% |
| M16 | ✅ | 13 (doc) | 100% |
| M17 | ⏳ | 5 code | 0% |
| **Итого** |  | **72** | **87.5%** |

> Документация: 63 завершено (M01→M13 + M15 + M16). Код: 5 задач (T1701–T1705, M17). 63/72 = 87.5% (документация), 0/5 = 0% (код).
